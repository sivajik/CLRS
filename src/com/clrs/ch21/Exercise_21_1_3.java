package com.clrs.ch21;

public class Exercise_21_1_3 {

	public static void main(String[] args) {
		/*
		 * for each edge (u,v) findset is called once for u and once for v. so if the
		 * edges are E then total 2*E we call findset <p> since each call to union
		 * reduce the total disjoine set by 1. so initially we have |v| number of
		 * disjoint sets.at the end |v|-k times union would be called.
		 */
	}
}
